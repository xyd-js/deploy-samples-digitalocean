spec:
  name: xyd-app
  services:
    - name: web
      environment_slug: static-site
      github:
        repo: xyd-js/deploy-samples
        branch: main
      build_command: bunx @xyd-js/cli build
      static_publish_path: .xyd/build/client
      routes:
        - path: /
          redirect:
            status_code: 301
            destination: /introduction/
        - path: /index.html
          redirect:
            status_code: 301
            destination: /introduction/
